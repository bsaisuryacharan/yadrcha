name: Refresh catalog

on:
  schedule:
    # Daily at 04:00 UTC (~09:30 IST) — quiet hours
    - cron: '0 4 * * *'
  workflow_dispatch:
    inputs:
      max_lookups:
        description: 'Max song-detail lookups this run'
        required: false
        default: '900'
      time_budget_min:
        description: 'Wall-clock budget in minutes (script self-stops before CI timeout)'
        required: false
        default: '40'

permissions:
  contents: write

concurrency:
  group: refresh-catalog
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade requests cryptography

      - name: Refresh catalog
        env:
          MAX_DETAILS: ${{ inputs.max_lookups || '900' }}
          # Script self-stops at this wall-clock budget so it always reaches
          # the save+commit step. Kept under timeout-minutes (50) as a net.
          TIME_BUDGET_MIN: ${{ inputs.time_budget_min || '40' }}
        run: python scripts/refresh_catalog.py

      - name: Commit changes
        # Run even if the refresh step errored — the script saves catalog.json
        # incrementally, so we still commit whatever fresh songs it banked.
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add catalog.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          # If main moved during this run, prefer the workflow's (newer)
          # catalog over whatever main has. The script always reads main's
          # catalog at start and merges new songs in, so its output is
          # the authoritative latest state.
          git stash
          git fetch origin
          git reset --hard origin/main
          git stash pop
          git add catalog.json
          git commit -m "chore: refresh catalog $(date -u +%Y-%m-%d) [skip ci]"
          git push
